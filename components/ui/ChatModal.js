// This component is no longer needed and has been replaced with a standalone chat page.
// The file is kept for reference but no longer used in the application.
// See pages/chat.js for the new implementation.

import React, { useState, useRef, useEffect } from 'react';
import { Dialog, Transition } from '@headlessui/react';
import { XMarkIcon, MinusIcon, ChatBubbleLeftRightIcon, ExclamationTriangleIcon, CheckCircleIcon, XCircleIcon } from '@heroicons/react/24/outline';
import Button from './Button';

export default function ChatModal() {
  // This component is deprecated. Use the /chat page instead.
  return null;
} 